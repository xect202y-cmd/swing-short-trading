# 단타 v3 — 갭반등·코스닥 (검증 결과 + 구현 스펙)

날짜: 2026-07-03 · 상태: 검증 완료, 사용자 승인 후 구현
선행: `2026-07-03-scalp-daytrading-design.md`(v1/v2), 개선 실험 결과 아래

## 0. 왜 v3인가 (검증된 발견)

v1(변동성 돌파)·v2(갭반등)를 볼트 우량주 유니버스에서 돌리면 둘 다 엣지 없음
(정정 후 v1 -0.077%/PF 0.92, v2 -0.026%/PF 0.98). 개선 실험(2026-07-03, 500일 OOS):

- **(A) v2 우량주 파라미터 정교화**: 갭 구간·손절폭·거래량 조건 10변형 → 최선 PF 1.01. **엣지 없음**.
- **(B) 코스닥 유니버스 확장**: v1 돌파는 코스닥에서도 PF~1.0. 그러나
  **v2 갭반등 × 코스닥 = IS +0.075% / OOS +0.356% / PF 1.28 / 445+516거래**. ✅

### 견고성 검증 (과최적화 아님을 확인)
- **인접 파라미터 전부 양수**: 손절 -2.0~-3.5%, 갭 임계 -1.5~-2.5%, 거래대금 20~50억,
  추세필터 유무 → 9변형 모두 OOS PF 1.14~1.30. 매직 파라미터 의존 없음.
- **종목 분산**: 최대 기여 종목 23%, 40종목 분산. 요행 아님.
- **파라미터 미변경**(v2 기본 룰 그대로) + 유니버스만 교체 → 커브피팅 원천 불가.

### 정직한 리스크 — 레짐 의존 (반드시 인지)
- 시간 3분할: +0.40%(24.9~25.10) → **−0.37%**(25.10~26.03, PF 0.73) → +0.65%(26.03~26.07).
- 평균회귀의 본질: 변동·횡보장에서 벌고 추세하락·저변동장에서 몇 달씩 흘림.
- **결론**: 통계적 엣지는 실재하나 다개월 드로다운 존재 → 페이퍼로 실전 데이터 축적하며
  나중에 지수 레짐 필터를 A/B 검증(성급한 필터가 엣지를 죽일 수 있으므로 무필터 시작).

## 1. 모델 정의

**단타 v3 — 갭하락 과매도 반등 (코스닥 중소형)**
- 로직 = 기존 v2와 **동일한 체결 룰**(시가 갭하락 -2%↓ 진입, 손절 -2.5% 진입가앵커,
  당일 종가 청산). settle_item의 v2 경로 재사용.
- 차이 = **유니버스**: 볼트 우량주가 아니라 코스닥 중소형 큐레이트 리스트.
- 필터: 거래대금 하한 `scalp.v3_min_tv_eok`(기본 30억, 코스닥 현실 반영), 전일 20>60일선.

## 2. 코스닥 유니버스 (생존편향 통제)

2024년 중반 시점에 이미 코스닥 대형/고유동이던 40종목(최근 수익률 무관 선정 →
생존편향 통제). pykrx 대량조회 API 차단으로 개별 fetch. 섹터 분산(반도체소부장·
2차전지·바이오·엔터·플랫폼). config.yaml `scalp.kosdaq_universe` 또는 코드 상수로 고정:

```
247540 086520 028300 196170 091990 066970 022100 277810 058470 240810
357780 112040 293490 095340 141080 067310 005290 039030 348370 078600
084850 137400 213420 056190 086900 108320 222080 025900 131970 393890
403870 145020 290510 214150 319660 036930 064760 178320 104830 263750
```
(유지보수: 분기 1회 재선정 권장. 종목 상폐/편출입은 수동 갱신.)

## 3. 구현 범위 (승인 후)

3-모델 아키텍처로 확장 — v1/v2(우량주)는 그대로 두고 v3(코스닥) 추가:

1. **account.py**: `_MODELS = ("v1","v2","v3")` + 시드 v3=300만(총 가상 900만). 멱등 정산 동일.
2. **strategy.py**: v3 items는 settle_item에서 v2(갭) 경로 사용 — model 필드를 "v3"로 두되
   갭 로직으로 라우팅(현재 `else` 분기를 `model in ("v2","v3")`로). k 불필요.
3. **planner.py**: v3 후보를 코스닥 유니버스에서 로드(볼트 아님) — 신규 로더 필요.
   시나리오/그림자 A/B 동일 적용.
4. **main.run_scalp**: KR 런에서만 v3 처리(코스닥=한국장). US 런은 v1/v2만.
5. **backtest.py + run_scalp_compare**: v3(갭반등·코스닥) 리플레이 추가 → scalp_compare.json
   versions에 "단타 v3" 항목. 대시보드는 versions 동적이라 자동 노출.
6. **briefer.py**: v3 결과/계획 라인 추가(⚡ 임베드에 v3 섹션).
7. **디스코드/볼트 기록**: 기존 경로에 v3 포함(자동).

## 4. 검증 계획

- 단위테스트: v3 갭 라우팅(settle_item model="v3" = v2 로직), 코스닥 로더, account 3모델 정산.
- `scalp-compare` → scalp_compare.json에 v3 항목, OOS PF>1.05 재확인(라이브 데이터로).
- 페이퍼 1사이클: v3 계획(코스닥 갭하락 후보) 디스코드 노출 확인.
- 대시보드: 비교화면 단타 카테고리에 v1/v2/v3 3개 노출.

## 5. 범위 밖 / 후속

- 지수 레짐 필터(무필터로 시작, 페이퍼 데이터 축적 후 A/B) — 성급 적용 금지.
- 코스닥 v1 돌파(엣지 없음 — 미채택).
- 우량주 v2 파라미터 변경(엣지 없음 — 미채택, 현행 유지).

## 부록: 실험 재현
스크래치패드 `scalp_exp2.py`(그리드), `scalp_v2_robust.py`(견고성). 데이터 1회 fetch 후
변형 비교, harness.split_oos/report_from_trades 재사용, 비용 1.5+5.0bps 고정.
